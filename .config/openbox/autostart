# Heavily derived from Crunchbang's.

## GNOME PolicyKit and Keyring
eval $(gnome-keyring-daemon -s --components=pkcs11,secrets,ssh,gpg) &

compton -b --unredir-if-possible

nitrogen --restore

xfce4-volumed
xfce4-power-manager
thunar --daemon &
xscreensaver -no-splash &

tint2 &
conky -q &

dropbox start
