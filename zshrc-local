# Fedora 32 stuff
if [[ $(uname) == "Linux" ]]; then
	# Fedora 32

	export MANROFFOPT="-c"

	# That's where I have the nnn prefix
	export PATH="$PATH:$HOME/.npm-global/bin"

	# That's where Fedora puts it
	source /usr/share/fzf/shell/key-bindings.zsh

	alias ls='ls --color=auto --classify'

	eval $(dircolors ~/.zsh/nord-dircolors/src/dir_colors)
fi

if [[ $(uname) == "Darwin" ]]; then

	source ~/.fzf.zsh
	source ~/.nvm/nvm.sh
	alias ls='gls --color=auto --classify'
	eval $(gdircolors ~/.zsh/nord-dircolors/src/dir_colors)
	export PATH=~/.local/bin:$PATH
fi

alias vim=nvim

export PATH=$PATH:~/go/bin

alias l='exa --icons'

# That's where I have my dotfiles
source ~/Documents/dotfiles/nnn_nord_theme.sh

export NNN_FIFO=/tmp/nnn.fifo
export USE_PISTOL=1
export NNN_PLUG="p:preview-tui"
export BAT_THEME=Nord

# These are literally what the Snowblock project set up.
export FZF_DEFAULT_COMMAND='fd --type file --follow --hidden --exclude .git'
export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"
export FZF_DEFAULT_OPTS='
--color bg:#2e3440,bg+:#4c566a,fg:#e5e9f0
--color fg+:#eceff4,hl:#88c0d0,hl+:#81a1c1
--color preview-bg:#2e3440,preview-fg:#e5e9f0
--color border:#4c566a,gutter:#3b4252,header:#88c0d0
--color info:#88c0d0,marker:#88c0d0,pointer:#81a1c1
--color prompt:#88c0d0,spinner:#88c0d0'

export MANPAGER="sh -c 'col -bx | bat --theme Nord -l man -p'"

source ~/.zsh/zsh-autosuggestions/zsh-autosuggestions.zsh
source ~/.zsh/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

export EDITOR=nvim

eval "$(starship init zsh)"
